const harryURL = "https://hp-api.herokuapp.com";

const harry = `${harryURL}/api`;
// const auth = `${url}/auth`;
// const account = `${url}/account`;

const ApiConfig = {
  allCharacter: `${harry}/characters`,
};

export default ApiConfig;
